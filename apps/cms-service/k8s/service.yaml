apiVersion: v1
kind: Service
metadata:
  name: cms-service
  namespace: staging

spec:
  type: NodePort
  selector:
    app: cms-service
  ports:
    - port: 80
      targetPort: 3002
      nodePort: 32082
